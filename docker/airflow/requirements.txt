# Thư viện cho DAG và module ETL, cài sẵn vào image thay vì cài lúc container chạy.
# Phiên bản ghim theo Python 3.8 của image Airflow 2.7.3.
boto3==1.34.162
psycopg2-binary==2.9.10
pandas==2.0.3
requests==2.31.0
sqlalchemy==1.4.54

# Nâng Celery/kombu so với bản đi kèm Airflow 2.7.3 (5.3.4 / 5.3.2). Bản cũ có lỗi:
# Redis khởi động lại là worker kết nối lại được nhưng KHÔNG BAO GIỜ lấy việc nữa
# (postmortem 5, sự cố 18/09). Tái hiện với một app Celery tối giản và Redis tạm:
# bản cũ 0/5 task chạy được sau 5 lần khởi động lại Redis, bản này 5/5.
celery==5.4.0
kombu==5.4.2
