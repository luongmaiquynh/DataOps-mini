---
# Chỉ cài Docker, không triển khai gì khác. Toàn bộ logic nằm trong role `docker`.
# Chạy: ansible-playbook install_docker.yml --ask-become-pass
- name: Install Docker Engine on all VMs
  hosts: all
  become: true
  roles:
    - docker
